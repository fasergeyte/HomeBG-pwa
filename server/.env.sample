# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

GOOGLE_CLIENT_ID=asdasdasd
GOOGLE_CLIENT_SECRET=asdasdasd
PORT=3000
BACK_BASE_URL=http://localhost:3000
DATA_DIR=/fgh/fgh
JWT_SECRET='some'